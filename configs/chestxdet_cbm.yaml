# Standard (non-spatial) Concept Bottleneck Model on the ChestX-Det subset.
# Loaded together with configs/chestxdet_base.yaml.

experiment: cbm

model:
  encoder: densenet121
  pretrained: true
  concept_activation: sigmoid

training:
  lambda_concept: 1.0
  lambda_diagnosis: 1.0
